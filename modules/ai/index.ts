// modules/ai/index.ts
// Public API for the AI module

// Types
export * from "./types";

// Hooks (client-side only)
export * from "./hooks";

// NOTE: Server-side exports (Router, Services) are in ./server/index.ts
// Import them separately: import { aiRouter } from "@/modules/ai/server"
